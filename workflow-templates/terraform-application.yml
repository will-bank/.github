name: Terraform Application
run-name: Deploy Terraform to ${{ inputs.ENVIRONMENT }} by ${{ github.actor }}

on:
  workflow_dispatch:
    inputs:
      path:
        description: 'Terraform Path'     
        required: false
        default: 'iac/terraform'
      environment:
        description: 'Environment' 
        required: true
        default: 'development'
        type: choice
        options:
          - 'development'
          - 'staging'
          - 'production'

jobs:
  workflow:
    uses: will-bank/reusable-workflows/.github/workflows/terraform-application.yml@main
    with:
      path: ${{ inputs.path }}
      environment: ${{ inputs.environment }}
